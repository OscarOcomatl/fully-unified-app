

import 'package:flutter/material.dart';

class BaksheeshScreen extends StatelessWidget {
   
  const BaksheeshScreen({Key? key}) : super(key: key);
  
  static const name = 'baksheesh-screen';  

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('BaksheeshScreen'),
      ),
    );
  }
}